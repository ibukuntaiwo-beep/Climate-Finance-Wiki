## 2026-07-18

**Session type:** Design composite (Option 9) — owner-directed merge of Option 3 + Option 1

**Branch/worktree:** `design/landing-reimagine` (worktree at `Websites/cfw-landing-redesign`).

**What changed:**
- Created: `mockups/landing-2026-07/index.html` — **Option 9 "The Composite"**, replacing the review gallery at this path (owner's request). Foundation is Option 3 (The Index); folded in from Option 1 (The Ledger): the featured-case hero block, the "wiki at a glance" stats, and the wide landscape plate.
- Created: `mockups/landing-2026-07/gallery.html` — the review gallery moved here, with a new #9 tile pointing at `index.html`.

**Option 9 structure (owner-specified):**
- Hero left: "How the world funds its response to climate change." moved to the top, with the featured case study (First Climate Change Bankruptcy + quote + CTA) underneath, separated by a hairline rule.
- Hero right: solar plate widened (was a thin strip) to a square archival plate, with mono "Wiki at a Glance" stat rows (count-up animation) beneath — the Ledger rail idea in Index styling.
- Below hero: the Ledger's pylons landscape plate as "Fig. 02 — Transmission"; caption swapped from the hero statement (now duplicated above) to the brand line "Making climate finance make sense — for the people who explain it." (owner approved the swap).
- 02 / The Index unchanged; the standalone Featured Case Study section removed (content now in hero); Trails renumbered 03, Dispatches 04; colophon/footer unchanged. All Ledger-sourced elements restyled into Index language (Space Grotesk, forest green, one accent).

**Bugs found & fixed:**
- Hero `align-items:end` left a large void above the headline — changed to `start`.
- The hero plate rendered at its raw `height=1000` attribute (CSS `aspect-ratio` is ignored when height isn't auto) — this is why Option 3's original strip looked too thin/tall. Fixed with `width:100%;height:auto;aspect-ratio:1/1`. Note: Option 3 itself still has this quirk; harmless there but worth fixing if Option 3 is ever ported.
- Mobile QC agent (CDP device-metrics, 320/390/768px): header overflowed below 357px (nav has a ~357px min-content floor). Fixed with a scoped `@media (max-width:356px)` that lets `.nav` wrap to two rows. All widths now pass with zero horizontal overflow.

**Verification:** desktop 1440px visually reviewed (3 iterations); mobile audited via CDP `Emulation.setDeviceMetricsOverride` with `mobile:false` (note: `mobile:true` silently expands the layout viewport and hides overflow on this build of headless Edge); lint greps pass (no emoji, no #000, no Inter, only verified Unsplash IDs, reduced-motion guard present); JS sanity pass (no console errors, all selectors/ids match).

**Follow-up revisions (owner-directed, same day):**
- Reading Trails (03) redesigned from the Index's row-list to the Ledger's three-column broadsheet layout (numbered columns, hairline dividers, "Begin →" links), restyled in Index language; single column below 900px.
- Removed "The Wiki at a Glance" stats from the hero's right column; the solar plate now fills that space (aspect ratio 1/1 → 3/4). Dead count-up JS removed.

**Open items:**
- Owner to review Option 9 at `mockups/landing-2026-07/index.html` (gallery now at `gallery.html`).
- Codex options 5–8 still pending; winner gets ported to `src/pages/index.astro`.
